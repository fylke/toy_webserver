# Copy this file to .env for local use.
# The compose stack reads these values automatically.

# Optional: keep fixed ports for a real class.
HOST_TCP_PORT=7777
HOST_HTTP_PORT=8080

# Optional: enable random host ports for local validation runs.
# RANDOMIZE_PORTS=1
